#include <stdio.h>
// this is also possible but not applicable for multiple statements after if. It is also not a good prectise.
int main() {
    int age=4;

    if(age<18)
    printf("adult");
    else
    printf("not eligible");

}